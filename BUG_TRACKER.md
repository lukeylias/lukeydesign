# Bug Tracker

A running list of bugs and issues found across the project.

---

## Bugs

- [ ] **Modal stacking click-outside behaviour** — When you open an image inside a modal (which creates its own modal), clicking outside both modals dismisses the background modal but keeps the image modal open. Expected: clicking outside should only dismiss the image modal.

## Improvements

- [ ] **Image handling inside modals** — General improvement needed for how images are displayed/handled when opened from within a modal.


- Loading needs some work, images render in really slow. I want this site to feel snappy
- I think we will need to finese the modal content, add more space bewteen elements.
- sounds and animations! will need to add them